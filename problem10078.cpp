// problem 10078
// Jonathon Simister

#include <cstdio>
#include <algorithm>

using namespace std;

int main() {
	pair<int, int> p[60];
	double cx, cy;
	int npoints;

	while(scanf("%d", &npoints) == 1) {
		if(npoints == 0) { break; }

		for(int c = 0;c < npoints;c++) {
			scanf("%d %d", &(p[c].first), &(p[c].second));
		}

		
	}

	return 0;
}