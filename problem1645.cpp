// problem 1645
// Jonathon Simister

#include <cstdio>
#include <cstring>

long long narr[1001][1001];
long long ntrees[1001];

long long arr(int v,int mts) {
	long long ret = 0;

	for(int n = 1;t < )
}

int main() {
	memset(ntrees, -1, sizeof(ntrees));
	memset(narr, -1, sizeof(narr));

	ntrees[0] = 1;
	ntrees[1] = 1;
	ntrees[2] = 1;
	ntrees[3] = 2;
}